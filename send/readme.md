# Panduan Instalasi (WMS App)

1. Buka terminal, lalu **masuk ke dalam folder** ini.
2. Load file container dengan perintah berikut:
   ```bash
   docker load -i WMS_Containers.tar
   ```
3. Terakhir, jalankan aplikasinya:
   ```bash
   docker-compose up -d
   ```